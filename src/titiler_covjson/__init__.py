"""titiler-covjson: CoverageJSON output format extension for TiTiler.

Public API lives in the submodules; import directly from them, e.g.
``from titiler_covjson.input import CoverageInput`` or
``from titiler_covjson.helpers import create_unit``. The package root
deliberately re-exports nothing, keeping the input, helpers, and (eventual)
modeler surfaces separate.
"""

__version__ = "0.1.0"
